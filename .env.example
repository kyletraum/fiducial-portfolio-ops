# Copy to .env and fill in. NEVER commit the filled copy - see .gitignore.
# Real values exist only on the machine that runs the stack.

POSTGRES_USER=fiducial
POSTGRES_PASSWORD=
POSTGRES_DB=fiducial_dev
